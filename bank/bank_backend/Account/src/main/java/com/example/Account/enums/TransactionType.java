package com.example.Account.enums;

public enum TransactionType {
    CREDIT,   // Money added to account (deposit, incoming transfer)
    DEBIT     // Money withdrawn from account (withdrawal, outgoing transfer)
}