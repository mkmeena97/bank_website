package com.example.Account.enums;

public enum AccountStatus {
    ACTIVE,     // Account is operational
    FROZEN,     // Account operations temporarily disabled
    CLOSED      // Account is closed and cannot be used
}

